# Спільні параметри шляхів для всіх доменів
IdParam:
  name: id
  in: path
  required: true
  description: Унікальний ідентифікатор (UUID)
  schema:
    type: string
    format: uuid

OrderIdParam:
  name: orderId
  in: path
  required: true
  description: Унікальний ідентифікатор замовлення (UUID)
  schema:
    type: string
    format: uuid

ItemIdParam:
  name: itemId
  in: path
  required: true
  description: Унікальний ідентифікатор предмета (UUID)
  schema:
    type: string
    format: uuid

ClientIdParam:
  name: clientId
  in: path
  required: true
  description: Унікальний ідентифікатор клієнта (UUID)
  schema:
    type: string
    format: uuid

BranchIdParam:
  name: branchId
  in: path
  required: true
  description: Унікальний ідентифікатор філії (UUID)
  schema:
    type: string
    format: uuid

PhotoIdParam:
  name: photoId
  in: path
  required: true
  description: Унікальний ідентифікатор фотографії (UUID)
  schema:
    type: string
    format: uuid

DocumentIdParam:
  name: documentId
  in: path
  required: true
  description: Унікальний ідентифікатор документа (UUID)
  schema:
    type: string
    format: uuid

CodeParam:
  name: code
  in: path
  required: true
  description: Унікальний код
  schema:
    type: string
    minLength: 1
    maxLength: 50
