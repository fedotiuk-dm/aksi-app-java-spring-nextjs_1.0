package com.aksi.mapper;

/**
 * Base mapper interface providing common mapping functionality.
 * This interface is meant to be extended by other mapper interfaces
 * to establish a common mapping hierarchy.
 */
public interface BaseMapper {
    // Base interface for all mappers
    // Specific mappings are now in specialized mapper classes
}
